%table
  %tr
    %th Account Name
    %th Transaction Count
  - @accounts.each do |account|
    %tr
      %td= link_to(account.name, account, :class => 'button tiny radius success')
      %td= account.transactions.count