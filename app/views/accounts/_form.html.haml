= form_for @account, :remote => true do |f|
  = f.label :name
  = f.text_field :name, :autofocus => true
  = f.label :balance
  = f.number_field :balance
  = f.submit
  = link_to 'Cancel', '#', :'data-clear-form' => true

